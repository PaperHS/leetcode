package main

import (
	"math/rand"
)

func main() {
	sss := []int{}
	for i := 0; i < 10000; i++ {
		sss = append(sss, rand.Intn(10000))
	}
	//	fmt.Println(sss)
}
func selectSort(sli []int) {
	for i := 0; i < len(sli); i++ {
		for j := i; j < len(); j++ {

		}
	}
}
